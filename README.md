# XML → Excel Converter

Преобразует XML-файлы в формат Excel (.xlsx) через веб-интерфейс или REST API.  
Приложение работает **без сохранения файлов на сервере** — загрузка и скачивание происходят в памяти.

![Java 17](https://img.shields.io/badge/Java-17-ED8B00?logo=java&logoColor=white)
![Spring Boot](https://img.shields.io/badge/Spring_Boot-3.2+-6DB33F?logo=spring&logoColor=white)
![Apache POI](https://img.shields.io/badge/Apache_POI-5.5+-3776AB?logo=apachepoi&logoColor=white)

---

## 🚀 Возможности

- 📥 Загрузка XML-файла через веб-форму
- ⚡ Потоковый парсинг XML (низкое потребление памяти)
- 📊 Генерация XLSX с корректным форматированием
- 🌐 REST API для интеграции
- 📄 Автоматическая документация (Swagger UI)
- 🔒 Безопасный парсинг (защита от XXE)

---

## 🛠 Требования

- Java 17+
- Maven 3.8+

---

## 🚦 Быстрый запуск

### Локально (для разработки)

```bash
  mvn clean spring-boot:run
```
## 🏗 Сборка и деплой
### Сборка JAR
```bash
  mvn clean package -Pprod -DskipTests
```
→ Получишь target/xml-to-excel-converter-*.jar

Запуск в production
```bash
  java -jar -Dspring.profiles.active=prod target/*.jar
```
🔒 В профиле prod:

- Отключён Swagger UI
- Минимальное логирование
- Запрещён вывод стек-трейсов в ответе


## 📁 Структура проекта
```
src/
├── main/
│   ├── java/
│   │   └── ru.oiteb.xmlparser/
│   │       ├── controller/     # REST API
│   │       ├── service/        # Бизнес-логика
│   │       ├── parser/         # Парсинг XML
│   │       ├── exporter/       # Генерация Excel
│   │       └── exception/      # Кастомные исключения
│   └── resources/
│       ├── static/             # Веб-интерфейс (HTML, CSS)
│       ├── application.yml     # Основная конфигурация
│       ├── application-dev.yml # Dev-профиль
│       └── application-prod.yml# Prod-профиль
└── test/                       # Unit и интеграционные тесты
```

## 🐳 Docker-развёртывание

### Сборка и запуск
```bash
    docker-compose up -d --build